<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>EEKbnb</title>
    <link rel='stylesheet' type='text/css' href='/main.css'>
    <link href="https://fonts.googleapis.com/css?family=Nixie+One|Hind|Emblema+One" rel="stylesheet">
  </head>
  <body>
    <div class='rowSec'>
      <% if current_user %>
        <div class='col-5 alert name' id='name'>
          <%= "Hello, #{current_user.first_name}" %>
        </div>
        <div class='col-7 flow_buttons'>
          <form action="/apartments/new">
            <input class='button' id='add_property' type="submit" value="Add a property">
          </form>
          <form action="/profile/requests">
            <input class='button' type='submit' value='Requests'>
          </form>
          <form action="/sessions" method="post">
            <input type="hidden" name="_method" value="delete">
            <input class='button' id='log_out' type="submit" value="Log out">
          </form>
        </div>
      <%else%>
        <div class='col-12 flow_buttons'>
          <form action="/sessions/new">
            <input class='button' type="submit" value="Sign in">
          </form>
          <form action="/users/new">
            <input class='button' type="submit" value="Sign">
          </form>
          <form action="/apartments">
            <input class='button' id='view apartments' type="submit" value="View listings">
          </form>
          <form action="/sessions/new">
            <input class='button' id='log in' type="submit" value="Login">
          </form>
        </div>
      <% end %>
    </div>




    <div class='rowSec'>
      <div class='col-12'>
        <% if flash[:errors] && !flash[:errors].empty? %>
          <div class='alert log_out_notice' id="log_out_notice">
            <span class="closebtn">&times;</span>
              <% flash[:errors].each do |error| %>
                <%= error %>
              <% end %>
          </div>
        <% end %>
      </div>
    </div>










    <script src="https://code.jquery.com/jquery-2.1.4.min.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>

    <%= yield %>
  </body>
</html>
